package rbf;

/**
 * Created by joshua on 9/22/15.
 */
public class RBFNeuralNetwork
{


    public int findCorrectAnswer(int[] inputs) {
        int result = 0;
        for (int i = 0; i < inputs.length - 1; i++) {
      //      result += (1 - inputs[i])*(1 - inputs[i]) + (100 * (inputs[i+1] );
        }
        return result;
    }
}
